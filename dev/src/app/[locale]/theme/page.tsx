'use client';

import { Header } from '@/components/DemoBlock';
import { ThemeProvider } from '@grace-studio/graceful-next/components';

const RootLayout = ({ children }: { children: React.ReactNode }) => (
  <div>Hello world</div>
);

export default RootLayout;
